import css from 'styled-jsx/css';

const styles = css`
  .search__container > div:not(:last-child) {
    margin-bottom: 16px;
  }

  .search__button {
    border: none;
    margin-right: 0px;
  }
`;

export default styles;
